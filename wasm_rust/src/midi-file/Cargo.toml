[package]
name = "midi-file"
version = "0.1.0"
authors = ["Poly <bartek.marynczak@op.pl>"]
edition = "2021"

[dependencies]
midly = "0.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
midi-io.workspace = true
